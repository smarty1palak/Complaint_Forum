<%= form_for(@complaint) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <div class="field">
    <%= f.text_area :content, placeholder: "Register new complaint" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>